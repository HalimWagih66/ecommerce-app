import 'package:flutter/material.dart';

Widget unDefinedScreen(){
  return Scaffold(
      body: Center(
        child: Container(
          child:  Text("unDefined Screen"),
        ),
      )
  );
}